// updateing the table results
$('#live-results').html('<%= escape_javascript(render partial: "debts", locals: {debts: @debts}) %>');
//$('.footable').footable();

// updating the map
//json = <%=raw @debts.to_gmaps4rails%>
//Gmaps.map.replaceMarkers(json)