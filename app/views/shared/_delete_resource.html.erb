
<% if !delete_resource.new_record? && can?(:delete, delete_resource) %>
    <%= link_to  eval(delete_resource.class.name.demodulize.downcase + 
    				"_path(#{delete_resource.id})"), 
    		title: t("delete"), method: :delete, data: 
    		{ confirm: "Are you sure you want to delete #{delete_resource.class.name.demodulize.downcase} '#{delete_resource.name}'?" }, 
			class: "btn-action glyphicons remove_2 btn-danger no-spinner" do%>
		<i></i>
	<% end %>
 <% end %>


